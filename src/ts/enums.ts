export enum Role {
  USER = "USER",
  SELLER = "SELLER",
}

export enum StoreStatus {
  ACTIVE = "ACTIVE",
  INACTIVE = "INACTIVE",
}

export enum DeliveryType {
  STANDARD = "STANDARD",
  EXPRESS = "EXPRESS",
}

export enum DeliveryStatus {
  PENDING = "PENDING",
  SHIPPED = "SHIPPED",
  CANCELLED = "CANCELLED",
  UNPAID = "UNPAID",
}
