export enum Role {
  USER = "USER",
  SELLER = "SELLER",
}

export enum StoreStatus {
  ACTIVE = "ACTIVE",
  INACTIVE = "INACTIVE",
}

export enum AccountStatus {
  ACTIVE = 'ACTIVE',
  INACTIVE = 'INACTIVE',
}

export enum Sort {
  ascending = 'ascending',
  descending = 'descending',
}
